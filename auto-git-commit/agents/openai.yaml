interface:
  display_name: "项目自动 Git 提交"
  short_description: "项目任务完成后检查生成物、维护忽略规则并安全提交"
  default_prompt: "使用 $auto-git-commit 完成项目代码任务，提交前检查本轮生成物、按需更新 .gitignore，并只提交本次任务产生的必要变更。"
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Gitwork"
  short_description: "为所选文件夹内所有项目文件改动强制执行安全 Git 收尾"
  default_prompt: "使用 $gitwork 作为所有项目文件改动的强制 Git 收尾流程；即使是从空文件夹创建一个小 HTML 项目，也要触发，并只提交可安全隔离的本次任务变更。"
policy:
  allow_implicit_invocation: true
